feat: Refactor InteractiveApp - Phase 3 & Testing

Finalized the scene-based refactoring of InteractiveApp.

Changes:
- Removed obsolete `AppMode` enum from `common_types.py`.
- Cleaned up debug and visualization scripts from `tests/`.
- Implemented comprehensive unit tests for `MapInteractionController`, `ScanningScene`, `FlashCalibrationScene`, `PpiCalibrationScene`, `MapGridCalibrationScene`, `IntrinsicsCalibrationScene`, and `ProjectorCalibrationScene`.
- Removed debug print statements from `calibration_scenes.py`.
- Ensured all 124 tests pass with the new architecture.